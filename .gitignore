# =========================
# 1. Операционные системы
# =========================
# Временные файлы и кэш Windows (чтобы они не мешались при коммитах)
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/

# Файлы macOS (на случай, если проект будет открываться на Маке)
.DS_Store
.AppleDouble
.LSOverride

# =========================
# 2. Редакторы кода и IDE
# =========================
# Внутренние настройки VS Code
.vscode/
!.vscode/settings.json # (кроме общих настроек проекта, если они нужны)

# Внутренние настройки PHPStorm / WebStorm
.idea/
*.iml
*.iws

# Настройки Sublime Text
*.sublime-workspace

# =========================
# 3. PHP и сервер
# =========================
# Зависимости Composer (если начнешь использовать сторонние библиотеки, 
# они не должны лететь в Git, они скачиваются командой composer install)
vendor/

# Логи ошибок PHP и веб-сервера (чтобы история не забивалась текстом ошибок)
*.log
error_log
php_errors.log
access.log

# =========================
# 4. Безопасность и доступы
# =========================
# Файлы с паролями от базы данных и локальными настройками!
# НИКОГДА не сохраняй пароли в публичный Git.
.env
.env.local
.env.*.local
config.local.php
db.php # (если у тебя есть отдельный файл с паролями, добавь его сюда)

# =========================
# 5. Файлы сайта (Картинки и кэш)
# =========================
# Папки с временными файлами кэша
cache/
tmp/

# Папки с картинками пользователей (новости, каталог, заглушки). 
# Раскомментируй (убери #) и укажи свои пути, если не хочешь хранить 
# тяжелые фото товаров и новостей в истории Git:
# uploads/
# assets/images/news/
# assets/images/catalog/
# =========================
# 6. Сборка и зависимости (Vite / Node.js)
# =========================
# Самая тяжелая папка, ее НИКОГДА не кладут в Git
node_modules/

# Папка с готовым сжатым сайтом после сборки
dist/

# Логи ошибок npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Кэш инструментов сборки
.eslintcache
.stylelintcache